# StudentApp 快速使用指南

## ✅ 项目已创建完成！

---

## 🚀 当前状态

**应用正在运行中**，监听以下端口：
- **HTTPS**: `https://localhost:7202` ✅（实验要求）
- **HTTP**: `http://localhost:5110`

---

## 📍 访问地址

### 学生管理页面（用于 JMeter 测试）

| 功能 | URL | HTTP 方法 |
|------|-----|----------|
| **学生列表** | `https://localhost:7202/StudentPage` 或 `/StudentPage/Index` | GET |
| **创建学生** | `https://localhost:7202/StudentPage/Create` | GET / POST |
| **查看详情** | `https://localhost:7202/StudentPage/Details?id=1` | GET |
| **编辑学生** | `https://localhost:7202/StudentPage/Edit?id=1` | GET / POST |
| **删除学生** | `https://localhost:7202/StudentPage/Delete?id=1` | GET / POST |

---

## 🧪 浏览器测试

1. 打开浏览器，访问：
   ```
   https://localhost:7202/StudentPage
   ```

2. 如果浏览器提示"连接不安全"或"证书错误"：
   - Chrome/Edge: 点击 **"高级"** → **"继续前往 localhost"**
   - Firefox: 点击 **"高级"** → **"接受风险并继续"**

3. 你应该看到学生列表页面，包含3个初始学生数据：
   - 张三（20岁）
   - 李四（22岁）
   - 王五（21岁）

4. 测试 CRUD 功能：
   - 点击 **"Create New"** 创建新学生
   - 点击 **"Edit"** 编辑学生信息
   - 点击 **"Details"** 查看详情
   - 点击 **"Delete"** 删除学生

---

## 🔧 JMeter 测试配置要点

### 1. 线程组配置

| 参数 | 第一组测试 | 第二组测试 |
|------|-----------|-----------|
| Number of Threads | 200 | 200 |
| Ramp-Up Period | 3 秒 | 10 秒 |
| Loop Count | 2 | 2 |

### 2. HTTP 请求默认值

- **Protocol**: `https`
- **Server Name**: `localhost`
- **Port Number**: `7202`

### 3. GET 请求配置

- **Name**: `GET-获取学生列表`
- **Method**: `GET`
- **Path**: `/StudentPage`

### 4. POST 请求配置

- **Name**: `POST-创建学生`
- **Method**: `POST`
- **Path**: `/StudentPage/Create`

**必填参数**（在 Parameters 中添加）：

| Name | Value | 说明 |
|------|-------|------|
| `Student.Name` | `测试学生` | 学生姓名 |
| `Student.Age` | `20` | 学生年龄 |
| `Student.Email` | `test@example.com` | 学生邮箱（可选） |

**⚠️ 重要**：POST 请求已禁用防伪令牌验证（`[IgnoreAntiforgeryToken]`），可直接测试。

---

## 🛑 停止应用

应用当前在后台运行。如需停止：

### 方法 1：通过终端
在 VS Code 终端中按 `Ctrl + C`

### 方法 2：通过任务管理器
1. 打开任务管理器（Ctrl + Shift + Esc）
2. 找到 `dotnet.exe` 进程
3. 结束任务

### 方法 3：通过 PowerShell 命令
```powershell
Get-Process -Name "dotnet" | Stop-Process -Force
```

---

## 🔄 重新启动应用

如果修改了代码或需要重启：

```powershell
cd "d:\myCollege\CS class\SE\work3_JMeter\StudentApp"
dotnet run --launch-profile https
```

---

## 📁 项目结构

```
StudentApp/
├── Data/
│   └── ApplicationDbContext.cs      # 数据库上下文
├── Migrations/                       # 数据库迁移文件
├── Models/
│   └── Student.cs                    # 学生模型
├── Pages/
│   └── StudentPage/                  # 学生管理页面（CRUD）
│       ├── Index.cshtml              # 列表页
│       ├── Create.cshtml             # 创建页
│       ├── Edit.cshtml               # 编辑页
│       ├── Details.cshtml            # 详情页
│       └── Delete.cshtml             # 删除页
├── Properties/
│   └── launchSettings.json           # 启动配置（端口7202）
├── appsettings.json                  # 配置文件（数据库连接字符串）
└── Program.cs                        # 程序入口
```

---

## 🗄️ 数据库信息

- **数据库类型**: SQL Server LocalDB
- **数据库名称**: `StudentDB`
- **连接字符串**: `Server=(localdb)\\mssqllocaldb;Database=StudentDB;Trusted_Connection=True;`
- **表名**: `Students`

### 查看数据库数据（可选）

使用 SQL Server Management Studio (SSMS) 或 Azure Data Studio：

**连接信息**：
- **Server**: `(localdb)\mssqllocaldb`
- **Authentication**: Windows Authentication

---

## 📊 JMeter 测试流程

### 步骤 1：保持应用运行
确保 `https://localhost:7202` 可访问

### 步骤 2：配置 JMeter
按照 `JMeter使用教程.md` 中的详细步骤配置

### 步骤 3：执行测试
- **第一轮**: Ramp-Up = 3秒
- **第二轮**: Ramp-Up = 10秒

### 步骤 4：记录数据
截图保存以下内容：
- Summary Report（聚合报告）
- View Results Tree（结果树）
- 线程组配置

### 步骤 5：分析指标
- **Average**（平均响应时间）
- **Throughput**（吞吐量 TPS）
- **Error %**（错误率）

---

## ⚠️ 常见问题

### Q1: 浏览器提示"无法访问此网站"
**解决**: 确认应用正在运行，查看终端是否显示 `Now listening on: https://localhost:7202`

### Q2: JMeter 测试全部失败（红色）
**可能原因**：
- 应用未运行
- URL 配置错误
- SSL 证书问题

**解决**：
1. 浏览器测试 URL 是否可访问
2. 检查 JMeter 的 Server Name 是否为 `localhost`
3. 检查端口号是否为 `7202`

### Q3: POST 请求返回 400 错误
**原因**: 参数缺失或格式错误

**解决**: 确认 Parameters 中添加了：
- `Student.Name`
- `Student.Age`

### Q4: 数据库连接失败
**解决**: 
```powershell
# 重新应用数据库迁移
cd StudentApp
dotnet ef database update
```

---

## 📞 下一步

1. ✅ **测试浏览器访问**: 确认 CRUD 功能正常
2. ✅ **配置 JMeter**: 按照教程创建测试计划
3. ✅ **执行性能测试**: 运行两组对比测试
4. ✅ **撰写实验报告**: 分析测试结果并生成 PDF

---

**项目创建时间**: 2025年12月16日  
**创建工具**: VS Code + .NET 8 SDK  
**测试端口**: HTTPS 7202 ✅
